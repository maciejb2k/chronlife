<%= form_with(model: admin_specialist_request) do |form| %>
  <% if admin_specialist_request.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_specialist_request.errors.count, "error") %> prohibited this admin_specialist_request from being saved:</h2>

      <ul>
        <% admin_specialist_request.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
